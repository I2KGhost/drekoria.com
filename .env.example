# SMTP server configuration used by the subscription endpoint.
# Provide the details of the mailbox that will send the confirmation email.
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=apikey
SMTP_PASSWORD=super-secret

# Address that appears as the sender of the confirmation email.
SUBSCRIPTION_FROM_EMAIL=updates@example.com

# Optional: shown in the confirmation email body.
SUBSCRIPTION_SITE_NAME=Drekoria
